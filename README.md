# 📘 ระบบเช็คชื่อเข้าเรียน (Attendance Management System)

ระบบจัดการการเข้าเรียนออนไลน์สำหรับสถาบันการศึกษา รองรับการเช็คชื่อ จัดการนักศึกษา อาจารย์ และรายวิชา

## ✨ ฟีเจอร์หลัก

### 👨‍🎓 สำหรับนักศึกษา
- ✅ เช็คชื่อเข้าเรียน (มาเรียน / มาสาย / ลา / ขาด)
- ✅ ดูประวัติการเข้าเรียน
- ✅ จัดการโปรไฟล์ส่วนตัว
- ✅ อัพโหลดเอกสารการลา

### 👨‍🏫 สำหรับอาจารย์
- ✅ จัดการรายวิชา (เพิ่ม ลบ แก้ไข)
- ✅ เช็คชื่อนักศึกษา
- ✅ ดูสถิติการเข้าเรียน
- ✅ จัดการโปรไฟล์อาจารย์

### 🔐 สำหรับแอดมิน
- ✅ Dashboard สถิติภาพรวม
- ✅ จัดการนักศึกษาและอาจารย์
- ✅ จัดการรายวิชาทั้งหมด

## 🛠️ เทคโนโลยีที่ใช้

### Frontend
- **React** - UI Library
- **React Router** - Routing
- **Axios** - HTTP Client
- **Tailwind CSS** - Styling
- **Lucide React** - Icons
- **SweetAlert2** - Alert/Modal

### Backend
- **Node.js** - Runtime
- **Express.js** - Web Framework
- **PostgreSQL** - Database
- **Multer** - File Upload

## 📁 โครงสร้างโปรเจกต์

```
attendance-system/
├── frontend/              # React Application
│   ├── src/
│   │   ├── pages/        # หน้าต่างๆ
│   │   ├── components/   # Components ที่ใช้ซ้ำ
│   │   └── App.jsx       # Main App
│   └── package.json
│
├── backend/              # API Server
│   ├── routes/          # API Routes
│   ├── config/          # Configuration
│   ├── middleware/      # Middleware
│   └── index.js         # Server Entry
│
└── README.md            # คู่มือนี้
```

## 🚀 การติดตั้งและรันโปรเจกต์

### ข้อกำหนดเบื้องต้น
- Node.js (v16 หรือสูงกว่า)
- PostgreSQL
- Git

### 1. Clone โปรเจกต์
```bash
git clone https://github.com/your-username/attendance-system.git
cd attendance-system
```

### 2. ติดตั้ง Dependencies

**Backend:**
```bash
cd backend
npm install
```

**Frontend:**
```bash
cd frontend
npm install
```

### 3. ตั้งค่า Database

สร้างฐานข้อมูล PostgreSQL:
```sql
CREATE DATABASE std_check;
```

สร้างตารางตามโครงสร้างในไฟล์ `database-schema.sql`

### 4. ตั้งค่า Environment Variables

สร้างไฟล์ `.env` ในโฟลเดอร์ `backend/`:
```env
PORT=5000
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_password
DB_NAME=std_check
```

### 5. รันโปรเจกต์

**Backend (Terminal 1):**
```bash
cd backend
npm start
# Server จะรันที่ http://localhost:5000
```

**Frontend (Terminal 2):**
```bash
cd frontend
npm run dev
# จะรันที่ http://localhost:5173
```

## 📊 Database Schema

### ตารางหลัก
- `students` - ข้อมูลนักศึกษา
- `professors` - ข้อมูลอาจารย์
- `courses` - ข้อมูลรายวิชา
- `attendance` - บันทึกการเข้าเรียน

## 🔐 การ Login

### บัญชีทดสอบ

**Admin:**
- Username: `admin`
- Password: `1234`

**นักศึกษา:** ลงทะเบียนผ่านหน้า Register

**อาจารย์:** ลงทะเบียนผ่านหน้า Register

## 🌐 API Endpoints

### Students
- `POST /create-std` - สร้างนักศึกษา
- `GET /students` - ดึงข้อมูลนักศึกษาทั้งหมด
- `GET /students/:id` - ดึงข้อมูลนักศึกษา
- `PUT /students/:id` - แก้ไขข้อมูลนักศึกษา
- `DELETE /students/:id` - ลบนักศึกษา

### Professors
- `POST /create-professor` - สร้างอาจารย์
- `GET /get-all-professors` - ดึงข้อมูลอาจารย์ทั้งหมด
- `GET /get-professor/:id` - ดึงข้อมูลอาจารย์
- `PUT /update-professor/:id` - แก้ไขข้อมูลอาจารย์
- `DELETE /delete-professor/:id` - ลบอาจารย์

### Courses
- `POST /create-subject` - สร้างรายวิชา
- `GET /get-all-subjects` - ดึงข้อมูลรายวิชาทั้งหมด
- `GET /get-subject/:id` - ดึงข้อมูลรายวิชา
- `PUT /update-subject/:id` - แก้ไขข้อมูลรายวิชา
- `DELETE /delete-subject/:id` - ลบรายวิชา

### Attendance
- `POST /check-class` - เช็คชื่อเข้าเรียน
- `GET /get-class-detail/:classId/:stdId` - ดูรายละเอียดการเข้าเรียน

### Dashboard
- `GET /get-dashboard-stats` - ดึงสถิติ Dashboard

## 📸 Screenshots

(เพิ่มภาพหน้าจอระบบที่นี่)

## 👥 ผู้พัฒนา

- **ชื่อของคุณ** - Full Stack Developer

## 📝 License

MIT License - คุณสามารถนำไปใช้และดัดแปลงได้อย่างอิสระ

## 🤝 การมีส่วนร่วม

ยินดีรับ Pull Requests! สำหรับการเปลี่ยนแปลงใหญ่ กรุณาเปิด Issue ก่อนเพื่อหารือ

## 📞 ติดต่อ

- GitHub: [@your-username](https://github.com/your-username)
- Email: your.email@example.com

---

⭐ ถ้าชอบโปรเจกต์นี้ กด Star ได้เลย!
